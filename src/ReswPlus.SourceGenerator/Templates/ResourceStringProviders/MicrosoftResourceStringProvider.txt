using Microsoft.Windows.ApplicationModel.Resources;

namespace _ReswPlus_AutoGenerated
{
    internal sealed class ResourceStringProvider
    {
        private readonly ResourceLoader _resourceLoader;

        public ResourceStringProvider(string resourceMapId)
        {
            _resourceLoader = new ResourceLoader(ResourceLoader.GetDefaultResourceFilePath(), resourceMapId);
        }

        public string GetString(string resourceId) => _resourceLoader.GetString(resourceId);
    }
}