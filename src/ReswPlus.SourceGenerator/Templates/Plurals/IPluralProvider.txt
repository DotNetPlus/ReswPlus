namespace _ReswPlus_AutoGenerated.Plurals
{
    /// <summary>
    /// Computes the CLDR plural category of a number for a given language.
    /// </summary>
    public interface IPluralProvider
    {
        /// <summary>
        /// Computes the plural category to use for the given quantity.
        /// </summary>
        /// <param name="n">The quantity to compute the plural category for.</param>
        /// <returns>The plural category matching <paramref name="n"/>.</returns>
        PluralTypeEnum ComputePlural(double n);
    }
}
