# Don't touch my line endings you git!
* text=auto

# Ignore all differences in line endings
*.jpg   binary
*.png   binary
*.gif   binary

# Data vendored from upstream, kept exactly as published so that refreshing it is a clean diff and the file
# can be checked against its source byte for byte.
tools/CldrRuleImporter/plurals.json -text