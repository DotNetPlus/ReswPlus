commit 566ecbe9833546a11f9da92ab6cdd19a15e5aa5a
Author: Rudy Huyn <Rudy.Huyn@microsoft.com>
Date:   Wed Aug 19 21:21:57 2026 -0700

    Make the Native AOT sample publishable on every architecture
    
    Three things stood between the sample and a deployable app.
    
    Building it for anything but x64 produced a project whose generated
    resource class did not exist, so the XAML compiler failed on every page
    that used it. The platform an app is built for is passed down to the
    projects it references, and the generator is a netstandard2.0 analyzer
    that has no platform: built as x86 or ARM64 it is an assembly Roslyn
    declines to load, and an analyzer that fails to load generates nothing.
    Every sample now pins that reference to AnyCPU. This was not specific to
    the new sample -- the existing ones were equally unbuildable for x86 and
    ARM64, and are fixed with it.
    
    Publishing produced an ordinary self-contained app rather than a native
    one: the AOT compiler is brought in by the restore, so asking for it only
    in the publish profile was asking too late, and the publish quietly fell
    back. It is declared in the project instead, together with the runtime
    identifiers it resolves against -- which is also what the earlier crash
    was really about. PublishAot without RuntimeIdentifiers is what produced
    an app that threw inside Windows.UI.Xaml before its first window, not
    Native AOT itself, so the correction is recorded where the wrong
    explanation was.
    
    Verified by publishing all three: win-x64, win-arm64 and win-x86 each
    produce a single native executable of a few megabytes with no assemblies
    beside it, and the resources compiled into it.
    
    Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>

diff --git a/samples/UWP/ReswPlusNativeAotUwpSample/ReswPlusNativeAotUwpSample.csproj b/samples/UWP/ReswPlusNativeAotUwpSample/ReswPlusNativeAotUwpSample.csproj
index 1473982..4301ea4 100644
--- a/samples/UWP/ReswPlusNativeAotUwpSample/ReswPlusNativeAotUwpSample.csproj
+++ b/samples/UWP/ReswPlusNativeAotUwpSample/ReswPlusNativeAotUwpSample.csproj
@@ -15,6 +15,7 @@
     <DefaultLanguage>en-US</DefaultLanguage>
     <LangVersion>latest</LangVersion>
     <Platforms>x86;x64;ARM64</Platforms>
+    <RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>
     <!--
       This is what makes the project a UWP project on modern .NET. It is also the only thing that tells
       ReswPlus what kind of app this is: unlike a UWP project built with .NET Native, this one carries no
@@ -35,11 +36,11 @@
 
   <PropertyGroup Condition="'$(Configuration)' == 'Release'">
     <!--
-      Native AOT is what replaces the .NET Native toolchain, and it is applied when the app is published
-      rather than when it is built: turning it on for an ordinary build produces an app that does not start.
-      It is declared in the publish profiles under Properties\PublishProfiles instead, so that building and
-      running the sample from the IDE keeps working.
+      Native AOT is what replaces the .NET Native toolchain a UWP app used to be compiled with. It only takes
+      effect when the app is published, and it has to be declared here rather than in the publish profiles so
+      that the restore brings the AOT compiler in with the rest of the assets.
     -->
+    <PublishAot>true</PublishAot>
     <TrimmerSingleWarn>false</TrimmerSingleWarn>
   </PropertyGroup>
 
@@ -48,7 +49,7 @@
   </ItemGroup>
 
   <ItemGroup>
-    <ProjectReference Include="..\..\..\src\ReswPlus.SourceGenerator\ReswPlus.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" Private="false">
+    <ProjectReference Include="..\..\..\src\ReswPlus.SourceGenerator\ReswPlus.SourceGenerator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" Private="false" SetPlatform="Platform=AnyCPU">
       <Project>{44d125b3-6c8a-4f2f-a76f-bffb2ad7e70d}</Project>
       <Name>ReswPlus.SourceGenerator</Name>
     </ProjectReference>
